name: Deploy to Preview Site (OLD Hostinger - Temporary)

on:
  workflow_dispatch:  # Manual trigger only
  push:
    branches:
      - preview  # Deploy when pushing to preview branch

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build site
        run: npm run build
        
      - name: List dist contents
        run: |
          echo "Contents of dist folder:"
          ls -la dist/
          
      - name: Deploy to OLD Hostinger Preview
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 185.212.71.198
          username: u921052894.mediumblue-chamois-837591.hostingersite.com
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp  # Using regular FTP
          port: 21
          local-dir: ./dist/
          server-dir: public_html/
          log-level: verbose
          state-name: ftp-preview-deploy-state.json