name: Deploy to OLD Hostinger (Fixed Username)

on:
  workflow_dispatch:
  push:
    branches: [ "test-old-deploy" ]  # Optional test branch

jobs:
  deploy-to-old:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Build site
        run: |
          npm ci
          npm run build
          
      - name: Deploy to OLD Hostinger with correct username
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 185.212.71.198
          username: u921052894.mediumblue-chamois-837591.hostingersite.com  # FULL USERNAME
          password: ${{ secrets.FTP_PASSWORD }}  # Same password as NEW
          protocol: ftps
          port: 21
          local-dir: ./dist/
          server-dir: domains/mediumblue-chamois-837591.hostingersite.com/public_html/
          state-name: ftp-deploy-old.json
          log-level: verbose